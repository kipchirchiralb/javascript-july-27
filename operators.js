// data operators

// arithmetic operators + - * / ** % 

const score = 90;

console.log(score + 3);
// TASK 1: What does the modulus operator (%) do?

// logic operators && || !  - and or not
 
console.log(score > 80 && score >100 ); 
console.log(!(score > 80 || score >100) ); 

// comparison operators > < >= <= == === != !==

console.log(score > 90);
 
const user = "albert"
console.log( score = 90 )
// = assignment operator
// == compares values only
// TASK 2: What is the difference between == and ===?   in js 

// 